set( CMAKE_FIND_ROOT_PATH_MODE_PACKAGE BOTH )
set( ROS_PYTHON_VERSION 3 )
SET( ROS_DISTRO iron )
# SET( CMAKE_SYSTEM_PROCESSOR aarch64 ) # geographiclib needs this and it would be overriden by the toolchain file
# set( NADIR_CROSS_COMPILE ON )